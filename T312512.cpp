#include <bits/stdc++.h>
using namespace std;
int T, n, k, m, arr[200001];
signed main() {
	cin >> T;
	while (T--) {
		scanf("%d %d %d", &n, &k, &m);
		for (int i = 1;i <= n;++i) {
			for (int j = 0;j <= m;++j) {
				arr[i] = i;
			}
		}
	}
	/*
	草 不会 真的 服了 这
	不会不会不会
	草
	*/
}